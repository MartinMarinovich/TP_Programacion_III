<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - PilchasAutoservicio</title>
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
    <div class="login-container">
        <h2>Iniciar Sesión</h2>
        <p style="text-align: center; color: #7f8c8d; margin-bottom: 20px;">Panel de Administración</p>
        
        <% if (error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
        <% } %>
        
        <form method="POST" action="<%= Endpoints.admin.auth.login %>">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>
            </div>
            
            <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" required>
            </div>
            
            <button type="submit" class="btn btn-primary">Ingresar</button>
            
            <button type="button" class="btn btn-secondary" onclick="autocompletar()">
                Acceso Rápido (Testing)
            </button>
        </form>
    </div>

    <script>
        function autocompletar() {
            document.getElementById('email').value = 'sysadmin@pilchas.com';
            document.getElementById('password').value = 'admin123';
        }
    </script>
</body>
</html>

